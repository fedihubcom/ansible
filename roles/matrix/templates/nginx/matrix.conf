server {
  listen 80;
  listen [::]:80;

  server_name {{ matrix__base_host }};

  set $CSP "";
  set $CSP "${CSP}default-src https://{{ matrix__base_host }};";
  set $CSP "${CSP}script-src 'none';";
  set $CSP "${CSP}object-src 'none';";
  set $CSP "${CSP}frame-src 'none';";
  set $CSP "${CSP}connect-src 'none';";
  set $CSP "${CSP}form-action 'none';";

  add_header Content-Security-Policy $CSP always;
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  return 301 https://$host$request_uri;
}

server {
  listen 443      ssl;
  listen [::]:443 ssl;

  server_name {{ matrix__base_host }};

  ssl_certificate     {{ matrix__nginx__ssl_cert }};
  ssl_certificate_key {{ matrix__nginx__ssl_key }};

  set $CSP "";
  set $CSP "${CSP}default-src https://{{ matrix__base_host }};";
  set $CSP "${CSP}script-src 'none';";
  set $CSP "${CSP}object-src 'none';";
  set $CSP "${CSP}frame-src 'none';";
  set $CSP "${CSP}connect-src 'none';";
  set $CSP "${CSP}form-action 'none';";

  add_header Content-Security-Policy $CSP always;
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  location /_matrix {
    proxy_pass http://localhost:8008;
    proxy_set_header X-Forwarded-For $remote_addr;

    client_max_body_size 10M;
  }

  location / {
    proxy_pass http://localhost:8000;
    proxy_set_header X-Forwarded-For $remote_addr;
  }
}

server {
  listen 8448      ssl;
  listen [::]:8448 ssl;

  server_name {{ matrix__base_host }};

  ssl_certificate     {{ matrix__nginx__ssl_cert }};
  ssl_certificate_key {{ matrix__nginx__ssl_key }};

  set $CSP "";
  set $CSP "${CSP}default-src https://{{ matrix__base_host }};";
  set $CSP "${CSP}script-src 'none';";
  set $CSP "${CSP}object-src 'none';";
  set $CSP "${CSP}frame-src 'none';";
  set $CSP "${CSP}connect-src 'none';";
  set $CSP "${CSP}form-action 'none';";

  add_header Content-Security-Policy $CSP always;
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  location / {
    proxy_pass http://localhost:8008;
    proxy_set_header X-Forwarded-For $remote_addr;
  }
}
